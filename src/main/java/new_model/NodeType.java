package new_model;

public enum NodeType {
    START,
    END,
    FOR,
    FOREACH,
    IF,
    WHILE,
    DOWHILE,
    DECLARATION,
    ASSIGN,
    METHOD,
    RETURN,
    BREAK,
    CONTINUE,
    TRY,
    CATCH,
    SWITCH,
    CASE,
    UNARY,
    BINARY,
    THROW,
    LABEL,
    FINALLY
}
